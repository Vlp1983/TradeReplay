import { StubPage } from "@/components/landing/stub-page";

export default function BacktestingPage() {
  return (
    <StubPage
      title="Options Backtesting"
      description="Point-in-time contract replay for options. Select a moment, view the chain, replay the contract. Coming soon."
    />
  );
}
